#!/usr/bin/env bash
# Legacy runner guard.  Use the Python companion CLI during development.
set -euo pipefail

cat >&2 <<'MESSAGE'
The legacy shell runner is disabled on this development branch.
Install the package and use `pihole-speedtest collect` after the Ookla CLI is available.
See README.md for the local development commands.
MESSAGE
exit 64
